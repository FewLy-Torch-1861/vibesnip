# VibeSnip

> ⚠️ **Warning: This project is VIBECODED.**
> It was entirely generated by an AI Agent in a "vibing" session. Code quality, security, and sanity are not guaranteed. Use at your own risk (and vibes).

A terminal-inspired, keyboard-centric code snippet manager.


## Tech Stack
*   **Backend:** Python (FastAPI)
*   **Frontend:** HTML + HTMX + TailwindCSS
*   **Database:** SQLite

## Setup & Run

1.  **Install Dependencies:**
    ```bash
    cd VibeSnip
    python3 -m venv .venv
    source .venv/bin/activate
    pip install -r requirements.txt
    ```

2.  **Run the App:**
    You can use the provided script to manage the server in the background:
    ```bash
    ./start.sh start
    ```
    To stop or restart the server:
    ```bash
    ./start.sh stop
    ./start.sh restart
    ```
    *Alternatively, for development with auto-reload:*
    ```bash
    uvicorn main:app --reload
    ```

3.  **Open in Browser:**
    Go to `http://127.0.0.1:8000`

## Features
*   **Add Snippets:** Store code, language, and tags.
*   **Search:** Instant filtering via HTMX.
*   **Copy:** One-click copy to clipboard.
*   **Delete:** Remove old snippets instantly.
